<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">

    <display-name>scm-web</display-name>

    <!--
        Coarse-grained web-tier gate: any authenticated holder of ANY
        application role may reach /api/*. Fine-grained, per-operation
        authorization (who can approve a customs document vs. merely track
        a shipment) is enforced by @RolesAllowed on the EJB methods behind
        each resource — this is deliberate defense-in-depth layering, not
        redundancy: the web constraint's job is only to ensure a Principal
        exists at all (so @RolesAllowed / isCallerInRole have something to
        check), while the EJB layer's @RolesAllowed remains the actual
        source of truth for what that Principal may do. See
        docs/CRITICAL_ANALYSIS.md, "Role-based access control
        implementation for logistics personnel."
    -->


    <security-constraint>
        <web-resource-collection>
            <web-resource-name>Supply Chain API</web-resource-name>
            <url-pattern>/api/*</url-pattern>
        </web-resource-collection>
        <auth-constraint>
            <role-name>ADMIN</role-name>
            <role-name>LOGISTICS_COORDINATOR</role-name>
            <role-name>CUSTOMS_AGENT</role-name>
            <role-name>WAREHOUSE_MANAGER</role-name>
            <role-name>VENDOR_REPRESENTATIVE</role-name>
            <role-name>CUSTOMER</role-name>
        </auth-constraint>
        <!-- Requires an HTTPS listener configured in GlassFish — see
             docs/DEPLOYMENT_GUIDE.md. Vendor data and trade documentation
             must not travel in cleartext over an untrusted network. -->
        <user-data-constraint>
            <transport-guarantee>CONFIDENTIAL</transport-guarantee>
        </user-data-constraint>
    </security-constraint>

    <security-role>
        <role-name>ADMIN</role-name>
    </security-role>
    <security-role>
        <role-name>LOGISTICS_COORDINATOR</role-name>
    </security-role>
    <security-role>
        <role-name>CUSTOMS_AGENT</role-name>
    </security-role>
    <security-role>
        <role-name>WAREHOUSE_MANAGER</role-name>
    </security-role>
    <security-role>
        <role-name>VENDOR_REPRESENTATIVE</role-name>
    </security-role>
    <security-role>
        <role-name>CUSTOMER</role-name>
    </security-role>
</web-app>
