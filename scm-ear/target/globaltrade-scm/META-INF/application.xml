<?xml version="1.0" encoding="UTF-8"?>
<application xmlns="https://jakarta.ee/xml/ns/jakartaee"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee https://jakarta.ee/xml/ns/jakartaee/application_10.xsd"
             version="10">

    <description>
        GlobalTrade Logistics Corporation — Supply Chain Management
        Platform. Bundles the EJB business tier (timer services,
        interceptor-based auditing/validation/monitoring, CMT and BMT
        transaction management, JAAS security, structured exception
        handling with dead-letter recovery) and the JAX-RS web tier behind
        one context root. See docs/CRITICAL_ANALYSIS.md for the
        architectural rationale and docs/DEPLOYMENT_GUIDE.md for deployment
        instructions.
    </description>
    <display-name>GlobalTrade SCM</display-name>

    <!--
        Module filenames here must match maven-ear-plugin's configured
        bundleFileName for each module (see scm-ear/pom.xml) — a mismatch
        here is a deploy-time failure, not a compile-time one, since this
        file and the plugin configuration are two independent places
        expressing the same fact.
    -->
    <module>
        <ejb>scm-ejb.jar</ejb>
    </module>
    <module>
        <web>
            <web-uri>scm-web.war</web-uri>
            <context-root>/scm</context-root>
        </web>
    </module>

    <security-role>
        <role-name>ADMIN</role-name>
    </security-role>
    <security-role>
        <role-name>LOGISTICS_COORDINATOR</role-name>
    </security-role>
    <security-role>
        <role-name>CUSTOMS_AGENT</role-name>
    </security-role>
    <security-role>
        <role-name>WAREHOUSE_MANAGER</role-name>
    </security-role>
    <security-role>
        <role-name>VENDOR_REPRESENTATIVE</role-name>
    </security-role>
    <security-role>
        <role-name>CUSTOMER</role-name>
    </security-role>
</application>
